#ifndef __WORLDDEFINITIONS__
#define __WORLDDEFINITIONS__

#include <GL/glew.h>
#include <GL/freeglut.h>
#include <GL/gl.h>

const GLint WINDOW_WIDTH = 500;
const GLint WINDOW_HEIGHT = 500;
const uint8_t MAX_STAGE = 5;

#endif